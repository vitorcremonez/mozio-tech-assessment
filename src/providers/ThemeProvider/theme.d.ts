export default interface Theme {
	colors: {
		primary: string;
		secondary: string;
		font: string;
		card: string;
		backgroundInput: string;
		fontInput: string;
		muted: string;
		danger: string;
	};
	sizing: {
		borderRadius: number;
	};
}
